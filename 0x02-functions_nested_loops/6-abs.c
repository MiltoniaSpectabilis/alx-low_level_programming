#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *
 * @num: random number.
 *
 * Return: num if positive number and -num if negative.
 */

int _abs(int num)
{

	if (num > 0)

	{
		return (num);
	}

	else

	{
		return (-num);
	}

}
